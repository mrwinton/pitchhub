development:
  # Configure available database sessions. (required)
  sessions:
    # Defines the default session. (required)
    default:
      # Defines the name of the default database that Mongoid can connect to.
      # (required).
      database: pitchhub_development
      # Provides the hosts the default session can connect to. Must be an array
      # of host:port pairs. (required)
      hosts:
        - localhost:27017
      options:
    secret_keeper_1:
      uri: mongodb://michael:Composed.fireball.27@lighthouse.5.mongolayer.com:10122,lighthouse.4.mongolayer.com:10122/secret_keeper_1?replicaSet=set-56105b5dc38ec3daf10015c8
      options:
    secret_keeper_2:
      uri: mongodb://michael:Composed.fireball.27@lighthouse.5.mongolayer.com:10123,lighthouse.4.mongolayer.com:10123/secret_keeper_2?replicaSet=set-56105e6bb59b798d0500162c
      options:

  # Configure Mongoid specific options. (optional)
  options:
test:
  # Configure available database sessions. (required)
  sessions:
    # Defines the default session. (required)
    default:
      # Defines the name of the default database that Mongoid can connect to.
      # (required).
      database: pitchhub_test
      # Provides the hosts the default session can connect to. Must be an array
      # of host:port pairs. (required)
      hosts:
        - localhost:27017
      options:
    secret_keeper_1:
      uri: mongodb://michael:Composed.fireball.27@c122.lighthouse.4.mongolayer.com:10122,lighthouse.5.mongolayer.com:10122,lighthouse.4.mongolayer.com:10122/secret_keeper_1?replicaSet=set-56105b5dc38ec3daf10015c8
      options:
    secret_keeper_2:
      uri: mongodb://michael:Composed.fireball.27@c123.lighthouse.4.mongolayer.com:10123,lighthouse.5.mongolayer.com:10123,lighthouse.4.mongolayer.com:10123/secret_keeper_2?replicaSet=set-56105e6bb59b798d0500162c
      options:

  # Configure Mongoid specific options. (optional)
  options:
#test:
#  sessions:
#    default:
#      database: pitchhub_test
#      hosts:
#        - localhost:27017
#      options:
#        read: primary
#        # In the test environment we lower the retries and retry interval to
#        # low amounts for fast failures.
#        max_retries: 1
#        retry_interval: 0
