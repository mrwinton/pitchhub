<!DOCTYPE html>
<html>
<head>
  <title>PitchHub</title>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title><%= @page_title or "PitchHub" %></title>
  <meta name="description" content="Innovating together!">

  <link rel="apple-touch-icon-precomposed" sizes="144x144" href="<%= asset_path 'pitchhub144.png' %>">
  <link rel="apple-touch-icon-precomposed" sizes="114x114" href="<%= asset_path 'pitchhub114.png' %>">
  <link rel="apple-touch-icon-precomposed" sizes="72x72" href="<%= asset_path 'pitchhub72.png' %>">
  <link rel="apple-touch-icon-precomposed" sizes="57x57" href="<%= asset_path 'pitchhub57.png' %>">

  <link rel="shortcut icon" href="<%= asset_path 'favicon.png' %>">

  <link href='http://fonts.googleapis.com/css?family=Courgette|Yellowtail' rel='stylesheet' type='text/css'>

  <%= stylesheet_link_tag    'vendor', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'head', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <%= yield :custom_head %>

</head>
<body style="background-color: #EAEDF1">
<!-- devise requires these to be in application.html.erb-->
<% if notice || alert %>
	<div class="hidden">
	  <div id="flash" class="<%= notice ? "notice" : ""%><%= alert ? "error" : ""%>">
		<% if notice %>
			<p><%= notice %></p>
		<% elsif alert %>
			<p><%= alert %></p>
		<% end %>
	  </div>
	</div>
<% end %>

<%= content_for?(:content) ? yield(:content) : yield %>

<%= javascript_include_tag 'vendor', 'data-turbolinks-track' => true %>
<%= javascript_include_tag 'shared/soon', 'data-turbolinks-track' => true %>

<%= yield :custom_body %>


</body>
</html>
