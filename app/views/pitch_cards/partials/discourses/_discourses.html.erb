
<% @discourses.each  do |d|

  point = PitchPointsHelper.pitch_points_hash.select { |point|
	d.pitch_point_name == point[:name]
  }

  point = point.any? ? point.first : nil

  # the pitch point must be specified in our pitch_point_hash
  if point == nil
	# TODO better error
	raise Error
  end

%>

	<%= render partial: 'pitch_cards/partials/discourses/pitch_point_discourse', locals: { point: point, discourse: d, pitch_card: @pitch_card } %>

<% end %>

<% if @discourses.any? %>
	<!--paginate @discourses, :remote => true-->
<% end %>